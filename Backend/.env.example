# Backend Environment Variables
# Copy this file to .env and fill in your actual values

# Stripe Keys (Get from https://dashboard.stripe.com/test/apikeys)
STRIPE_SECRET_KEY=sk_test_51YOUR_SECRET_KEY_HERE
STRIPE_WEBHOOK_SECRET=whsec_YOUR_WEBHOOK_SECRET_HERE

# Gmail (for email sending)
GMAIL_USER=your-email@gmail.com
GMAIL_APP_PASSWORD=your-app-password

# Notes:
# 1. For STRIPE_WEBHOOK_SECRET during local development:
#    Run: stripe listen --forward-to http://localhost:8080/api/stripe/webhook
#    Copy the webhook signing secret from the CLI output
#
# 2. Never commit the actual .env file to git!
#
# 3. For production, use live keys (sk_live_...) and set up webhooks
#    in the Stripe Dashboard pointing to your production URL
